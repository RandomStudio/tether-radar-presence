const defaults = {
  loglevel: "info",
  serial: {
    path: "/dev/tty.usbserial-022F04E1",
    baudRate: 115200,
  },
  tetherRole: "PresenceRadarBridge",
  tether: {
    host: "localhost",
  },
};

export default defaults;
